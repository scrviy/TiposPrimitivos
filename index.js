// string
const nome = 'Junior';
const nome1 = "Junior";
const nome2 = `Junior`; 

// number
const num1 = 10; //number
const num2 = 10.52; //number

//undefined and null -> não aponta para nenhum local na memória.
let nomeAluno;
let sobrenomeAluno = null;

// boolean => false || true
const aprovado = false;